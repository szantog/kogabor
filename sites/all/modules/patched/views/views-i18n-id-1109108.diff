diff -rup views.orig//includes/handlers.inc views//includes/handlers.inc
--- views.orig//includes/handlers.inc	2011-03-23 01:21:08.000000000 +0100
+++ views//includes/handlers.inc	2011-03-29 11:28:09.227060000 +0200
@@ -289,7 +289,9 @@ class views_handler extends views_object
       $display_id,
       $this->handler_type,
       $options['table'],
-      $options['field']
+	  // NOTE: use (unique) id instead of field
+      // $options['field']
+	  $options['id']
     );
 
     $this->unpack_options($this->options, $options);
@@ -313,7 +315,11 @@ class views_handler extends views_object
         $this->real_field = $options['field'];
       }
     }
-
+    // not sure if this is required
+	if (isset($options['id'])) {
+		$this->id = $options['id'];
+	}
+	
     $this->query = &$view->query;
   }
 

diff -rup views.orig//plugins/views_plugin_display.inc views//plugins/views_plugin_display.inc
--- views.orig//plugins/views_plugin_display.inc	2011-03-23 01:21:08.000000000 +0100
+++ views//plugins/views_plugin_display.inc	2011-03-29 10:48:18.940055200 +0200
@@ -2582,10 +2581,12 @@ class views_plugin_display extends views
       else {
         $handler_type = $type;
       }
+	  
       $handler = views_get_handler($info['table'], $info['field'], $handler_type);
+	  // Note: use (unique) $info['id'] instead of $info['field']
       if ($handler) {
         $handler->init($this->view, $info);
-        $handler->unpack_translatables($translatable, array_merge($parents, array($handler_type, $info['table'], $info['field'])));
+        $handler->unpack_translatables($translatable, array_merge($parents, array($handler_type, $info['table'], $info['id'])));
       }
 
       // Prevent reference problems.

